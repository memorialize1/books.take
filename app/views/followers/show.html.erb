<div class = "container">

  <div class = "row">

    <div class = "col-md-3">

      <h2>User info</h2>
      <%= render 'info', user: @user %>
      <h2 class="mt-3">New book</h2>
      <%= render 'form', book: @create %>

    </div>

    <div class = "col-md-8 offset-md-1">
      <table class = "table">
        <h1>
        👤フォロワー<%= @followers.count %>人
        <% @followers.each do |follow| %>
        </h1>
        <tr>

          <td>
            <%= attachment_image_tag(follow, :profile_image, :fill, 50, 50, fallback: "no-image-icon.jpg") %>
          </td>

          <td><%= follow.name %></td>

          <td><%= link_to 'Show', user_path(follow) %></td>


          <td>
          </td>


        </tr>

        <% end %>

      </table>
    </div>

  </div>
</div>

